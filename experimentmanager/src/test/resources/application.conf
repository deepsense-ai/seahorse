defined.in.all.confs=application
defined.in.application.conf=application
overridden.by.system.prop=application
server.name=NOT_CONFIGURED
server.url="https://"${server.name}
server.host=localhost
server.port=9080
db.migrateOnStartup=false
db.user=""
db.password=""
db.url=""
experiments.api.prefix=v1/experiments
operations.api.prefix=v1/operations

auth-service.endpoint=""
auth-service.identity=""
auth-service.password=""

roles {
  experiments {
    get = "experiments:get"
    update = "experiments:update"
    create = "experiments:create"
    list = "experiments:list"
    delete = "experiments:delete"
    launch = "experiments:launch"
    abort = "experiments:abort"
  }
}
