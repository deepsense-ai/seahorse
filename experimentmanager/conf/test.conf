include "application.conf"

test {
  restapi {
    userA {
      identity = "integrationtestsA:itUserA"
      password = "itUserPass"
    }
    userB {
      identity = "integrationtestsB:itUserB"
      password = "itUserPass"
    }
  }
}
