# Copyright (c) 2015, CodiLime Inc.

- name: "Pull \"{{docker_image}}\" Docker image"
  docker:
    name: "{{docker_image}}"
    image: "{{docker_image}}"
    state: present

- name: "Export \"{{docker_image}}\" Docker container"
  shell: "docker export {{docker_image}} | gzip -c > {{download_dir}}/{{docker_image}}-docker.tgz"
