# Copyright (c) 2016, CodiLime Inc.

---
dependencies:
- { role: supervisedprogram,
    program: {
      name: 'SessionManager',
      command: '{{install_dir}}/deepsense-sessionmanager/bin/deepsense-sessionmanager -Dconfig.file={{install_dir}}/application.conf -DlogFile=/home/ubuntu/log/deepsense/sm/deepsense-sessionmanager',
      directory: "{{install_dir}}",
      autostart: true,
      autorestart: true,
      startretries: 3,
      stdout_logfile: "{{install_dir}}/std.out",
      stdout_logfile_maxbytes: 0,
      stderr_logfile: "{{install_dir}}/std.err",
      stderr_logfile_maxbytes: 0,
      user: ubuntu,
    }
  }
