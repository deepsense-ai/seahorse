# Unit tests for system test library

This test suite assumes you have development environment installed
and configured ds-dev-env-master host (to support HDFS).

To run tests, execute:

```
python -m unittest discover
```
